import { Container, Text, Title } from '@mantine/core';

export default function App() {
    return (
        <Container py="xl">
            <Title order={1}>Application Template</Title>
            <Text mt="sm">Ready for the first use case.</Text>
        </Container>
    );
}
